one:
  name: name_one
  host: http://host/one
  proxy: http://proxy
other:
  name: name_other
  host: http://host/other
output:
  encoding: utf8
  response_dir: tmpdir
addons:
  log:
    name: addons.log.csv-addon
    config:
      encoding: utf8
