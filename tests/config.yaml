one:
  name: name_one
  host: http://host/one
  proxy: http://proxy
other:
  name: name_other
  host: http://host/other
input:
  encoding: utf8
  format: csv
output:
  encoding: utf8
  response_dir: tmpdir
