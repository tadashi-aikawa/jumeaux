one:
  name: name_one
  host: http://host/one
  proxy: http://proxy
other:
  name: name_other
  host: http://host/other
output:
  encoding: utf8
  response_dir: responses
addons:
  log2reqs:
    name: addons.log2reqs.csv
    config:
      encoding: utf8
